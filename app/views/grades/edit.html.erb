<h1>Editing grade</h1>

<%= error_messages_for :grade %>

<% form_for(:grade, :url => course_grade_path(@course, @grade), :html => { :method => :put }) do |f| %>
  <p>
    <%= submit_tag "Update" %>
  </p>
<% end %>

<%= link_to 'Show', course_grade_path(@course, @grade) %> |
<%= link_to 'Back', course_path(@course) %>
