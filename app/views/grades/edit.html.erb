<h1>Editing grade</h1>

<%= error_messages_for :grade %>

<% form_for(:grade, :url => grade_path(@grade), :html => { :method => :put }) do |f| %>
  <p>
    <%= submit_tag "Update" %>
  </p>
<% end %>

<%= link_to 'Show', grade_path(@grade) %> |
<%= link_to 'Back', grades_path %>