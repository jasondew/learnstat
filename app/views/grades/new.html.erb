<h1>New grade</h1>

<%= error_messages_for :grade %>

<% form_for(:grade, :url => user_course_grades_path(current_user, @course)) do |f| %>
  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>

<%= link_to 'Back', course_path(@course) %>
