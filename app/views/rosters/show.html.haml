%h2 Roster

%table
  %thead
    %tr
      %th Student
      %th Login
      %th Blackboard ID
      %th Last Logged On
      %th Actions

  %tbody
    - @students.each do |student|
      %tr
        %td= mail_to student.email, student.name
        %td= student.login
        %td= student.blackboard_username
        %td= datetime_format student.last_login
        %td
          = link_to_remote 'Reset Password', :url => reset_password_course_user_path(:course_id => @course, :id => student.id), :success => "alert(request.responseText)"
          = link_to 'Impersonate', impersonate_course_roster_path(:course_id => @course, :id => student.id), :method => :post
